package dev.jbosscli.psi

import com.intellij.psi.tree.IElementType
import dev.jbosscli.JBossCliLanguage

class JBossCliElementType(debugName: String) : IElementType(debugName, JBossCliLanguage)
class JBossCliTokenType(debugName: String) : IElementType(debugName, JBossCliLanguage)
